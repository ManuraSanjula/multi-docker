sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t manurasanjula2003/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true manurasanjula2003/react-test npm test

after_success:
  - docker build -t manurasanjula2003/multi-client ./client
  - docker build -t manurasanjula2003/multi-nginx ./nginx
  - docker build -t manurasanjula2003/multi-server ./server
  - docker build -t manurasanjula2003/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push manurasanjula2003/multi-client
  - docker push manurasanjula2003/multi-nginx
  - docker push manurasanjula2003/multi-server
  - docker push manurasanjula2003/multi-worker